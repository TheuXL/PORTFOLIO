/* awsConfig.ts
import AWS from 'aws-sdk';

// Configuração da região e credenciais da AWS
AWS.config.update({
  region: 'us-west-2', // Substitua pela sua região preferencial
  accessKeyId: 'AKIDEXAMPLE', // Substitua com sua accessKeyId
  secretAccessKey: 'SECRETKEYEXAMPLE', // Substitua com sua secretAccessKey
});

export default AWS;

*/